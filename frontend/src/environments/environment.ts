export const environment = {
  production: false,
  /** Proxied to the NestJS API (see proxy.conf.json). */
  apiUrl: '/api',
  /** Socket.IO gateway endpoint. */
  socketUrl: 'http://localhost:3000',
};
